/** Пароль админки (только демо на клиенте). */
export const ADMIN_PASSWORD = "velvet2026";

/** POST с { phone, code } для реальной отправки SMS; пусто = демо-код на экране. */
export const SMS_SEND_ENDPOINT = "";
